# NeuralScreen

> **Форк с экспериментальной поддержкой HDR:** автоматический захват FP16 и вывод scRGB.
> Нейросеть обрабатывает SDR-копию; записи и скриншоты остаются SDR.
> [Установка, проверки и ограничения](docs/HDR.md).

**Нейронный рендерер NVIDIA из DLSS 5 — на весь рабочий стол Windows, в
реальном времени.** Всё, что на экране — игры, видео, фотографии — проходит
через ту же нейросеть, что работает в играх с DLSS 5, и возвращается чётче.

> **Сначала прочитайте это:** краткий гайд ниже запустит вас. Полная
> техническая история — как это работает, что замерялось, почему так
> устроено — в **[TECHNICAL.ru.md](TECHNICAL.ru.md)**. English:
> **[README.md](README.md)** / **[TECHNICAL.md](TECHNICAL.md)**. Русская
> версия: **[README.ru.md](README.ru.md)**.

<table>
<tr>
<td><img src="https://raw.githubusercontent.com/perseval-BLR/DLSS5-NeuralScreen/main/docs/screenshot-main-light.png" alt="Меню, светлая тема" width="420"></td>
<td><img src="https://raw.githubusercontent.com/perseval-BLR/DLSS5-NeuralScreen/main/docs/screenshot-main-dark.png" alt="Меню, тёмная тема" width="420"></td>
</tr>
</table>

*Всё собрано в одно меню внутри оверлея. Ползунок **«Шторка до / после»**
делит экран пополам, чтобы было видно, что именно делает эффект.*

## Что нужно

- **Windows 11** или Windows 10 - работает по отзывам, см. ограничения
- **Видеокарта NVIDIA RTX.** Честно, что реально работает:

  | Карты | Статус |
  |---|---|
  | **RTX 50** (Blackwell) | ✅ работает - официально поддерживаемое поколение |
  | **RTX 40** (Ada) | ✅ работает - через встроенный хук архитектуры |
  | **RTX 30** (Ampere) | ✅ работает - возвращено в v1.5.1 (универсальный рантайм + спуф 0x1B0, тот же стек, что в v1.3.0) |
  | **RTX 20** (Turing) | ❌ нейропроход не может работать в принципе - ниже минимальной архитектуры (DLSS5-Feeder issue #73). См. «Проба на RTX 20» ниже. |
  | **Ноутбуки с гибридной графикой (Optimus)** | ✅ работает - на дисплее встроенной графики захват переключается на GDI (медленнее; подтверждено на Acer Nitro с RTX 4050) |

- **Ничего устанавливать не надо.** В архиве релиза есть свой Python.

### Проба на RTX 20

Turing ниже минимальной архитектуры - ни одна сборка рантайма не запустит
нейропроход (0xBAD00001, FeatureNotSupported). Программа запустится, меню
работает; картинка не обрабатывается.

## Установка

1. Скачайте архив со страницы [Releases](https://github.com/perseval-BLR/DLSS5-NeuralScreen/releases)
   и распакуйте куда угодно. **Внутри уже всё есть** — включая
   `nvngx_dlssnr.dll` от NVIDIA (158 МБ, слишком большой для репозитория
   GitHub, поэтому он едет в архиве).
2. Запустите **`NeuralScreen.exe`**.

При первом запуске Windows скорее всего предупредит о неизвестном издателе —
программа не подписана платным сертификатом. Нажмите *Подробнее* → *Выполнить
в любом случае*. Не хотите — рядом лежит `NeuralScreen.vbs`, он делает то же
самое.

Установщика нет, за пределами папки ничего не создаётся. Чтобы удалить —
удалите папку.

> **Не используйте в соревновательных онлайн-играх.** Процесс с именем
> `nvngx.dll` плюс полноэкранный оверлей — ровно то, что ищут античиты.

## Как пользоваться

Программа живёт в трее (с кнопкой в панели задач) и рисует поверх
рабочего стола. **Num2** — меню. Клавиши сидят на нампаде, поэтому
**Num Lock должен быть включён** — с выключенным эти клавиши посылают
Insert/End/стрелки, и ничего не произойдёт.

| Клавиша | Что делает |
|---|---|
| **Num2** | открыть / закрыть меню |
| **Num1** | нейронная обработка вкл / выкл |
| **Num3** | скриншот |
| **Num0** | начать / остановить запись, со звуком |
| **Num4** / **Num6** | разрешение обработки ниже / выше |
| **Num5** | захватить окно под курсором (см. ниже) |
| **Ctrl+Alt+Q** | выход |

Любую клавишу можно переназначить в меню, под иконкой ползунков. Пока меню
открыто, оно забирает мышь и клавиатуру — поэтому работает поверх игры.
Закрытое, оно пропускает клики насквозь, как будто его нет.

Запуск и переключения не мигают: оверлей появляется с первым настоящим
кадром, а перестройку конвейера прикрывает короткая блур-заглушка со
спиннером.

### Весь экран или одно окно

NeuralScreen обрабатывает весь экран. Хотите эффект только на одном окне
(игра, браузер)? Выберите **«Выбрать окно...»** в меню - список покажет
все открытые окна, наведение подсветит нужное на экране, и оверлей будет
следовать за окном. Вернуться на весь экран - **«Во весь экран»** в меню.

**Num5** - быстрый путь для частого случая: нужное окно уже открыто и
занимает весь экран - наведите на него и нажмите **Num5**, чтобы захватить
его напрямую. Во всех остальных случаях используйте список окон в меню.

Зачем одно окно: внешняя запись. В полноэкранном режиме оверлей прячется
от захвата экрана (иначе программа снимала бы саму себя), поэтому OBS и
NVIDIA App его не видят. В режиме окна - видят (см. «Запись и скриншоты»).
Свёрнутое окно ставит обработку на паузу.

## Меню

<table>
<tr>
<td><img src="https://raw.githubusercontent.com/perseval-BLR/DLSS5-NeuralScreen/main/docs/screenshot-windows.png" alt="Список окон" width="380"></td>
<td><img src="https://raw.githubusercontent.com/perseval-BLR/DLSS5-NeuralScreen/main/docs/screenshot-settings.png" alt="Страница настроек" width="380"></td>
</tr>
</table>

Точка рядом с названием видеокарты зелёная, когда обработка на ней реально
работает, и красная, когда нет.

Что стоит покрутить:

- **Профиль** — насколько сильный эффект, от *Faithful* до *Extreme*. По
  умолчанию *Natural* — честный, сбалансированный вид. Четыре ползунка под
  ним — то же самое, но в деталях. **Save preset** сохраняет текущие
  значения ползунков под именем (Preset 1, Preset 2, ...) — пресет
  появляется в списке Profile и применяется как встроенный профиль;
  **Delete preset** удаляет активный пользовательский пресет. Тёмные сцены
  обрабатываются автоматически: адаптивная экспозиция подсвечивает их для
  сети, так что тени сохраняют детали вместо артефактов.
- **Шторка до / после** — оставляет левую часть экрана необработанной, чтобы
  было видно, что именно делает эффект. Насмотрелись — верните на 0.
- **Разрешение работы сети** — один ползунок. В верхнем положении это весь ваш
  экран: так по умолчанию и так картинка лучше всего. Каждый шаг вниз отдаёт
  сети кадр поменьше: при выключенном ползунке сеть уже работает на максимуме,
  а каждый шаг вниз даёт **примерно на 50% больше кадров** на 2560×1440 при 4K
  экране. Картинка при этом остаётся резкой: результат сети ложится на
  исходный кадр 1:1 (matched residual composite), так что текст, края и
  интерфейс сохраняют полное разрешение, а дешёвая сеть на пониженном
  разрешении занимается пересветом. Посмотрите на свой экран и выберите шаг.
Интерфейс говорит на **12 языках** — английский, русский, французский,
немецкий, испанский, итальянский, португальский, польский, украинский,
китайский, японский и корейский. Выбирается за иконкой ползунков (Language).
Меню, HUD и уведомления переключаются вместе.

Всё остальное — какой монитор, открывать ли меню при запуске, автозапуск с
Windows, назначение клавиш — за иконкой ползунков.

## Запись и скриншоты

**Num0** записывает то, что вы видите, с системным звуком, в MP4 в
папке `recordings`. **Num3** — скриншот. Открытое меню попадёт в оба — так
задумано. Пока идёт запись, в углу экрана висит красная точка с таймером
(отключается в настройках). Звук записи проходит через мягкий лимитер,
так что громкий системный микс не даст клиппинга и искажений.

Скриншоты открывают диалог **Save As**; выберите папку один раз через
**Screenshot folder...** в настройках — и диалог будет каждый раз
открываться в ней.

**Внешняя запись обработанной картинки:**
- **OBS (рекомендуется):** включите **Вывод в Spout2 (OBS)** в настройках
  (секция RECORDING). Воркер публикует вывод как Spout2 shared texture -
  добавьте в OBS источник **Spout2 Capture** (бесплатный плагин) и
  записывайте в AV1 / HEVC / H.264 через NVENC. Работает и в полноэкранном
  режиме. По умолчанию выключено.
- **NVIDIA App / OBS display capture:** используйте режим одного окна -
  выберите окно в меню (или наведите на него и нажмите **Num5**),
  записывайте, затем вернитесь на **«Во весь экран»** когда закончите.
  У NVIDIA App нет входа Spout, поэтому это его единственный путь. В
  этом режиме оверлей виден захвату экрана; в полноэкранном - прячется
  (программа снимает экран сама, и видимый оверлей питался бы собой).

## Если что-то не работает

**После запуска ничего не появилось.** Посмотрите `NeuralScreen.log` рядом с
программой; чаще всего не хватает `native\nvngx_dlssnr.dll`.

**В игре оверлей не виден.** Поверх игры в настоящем полноэкранном режиме
Windows не рисует ничего — это правило системы. Переключите игру в
*безрамочный* или *оконный полноэкранный* режим.

**Указатель меню пропал или замер.** Полноэкранная игра прячет системный
курсор; оверлей показывает только системный курсор. Лечится безрамочным
режимом.

**Картинка мягкая.** Верните ползунок «Разрешение работы сети» в верхнее
положение.

**Клавиша не срабатывает.** Её занял кто-то другой; переназначьте в меню
под иконкой ползунков.

**Меню медленно реагирует в тяжёлой игре.** На 4K конвейер может тратить до
секунды на кадр; нажатие может показаться потерянным.

## Известные ограничения
- **HDR-дисплеи:** в этом форке добавлена экспериментальная поддержка; [установка и ограничения](docs/HDR.md).
- **Windows 10, две видеокарты NVIDIA и второй монитор — экспериментально**: сделано по логам пользователей, ничего из этого не проверено на машине разработки (Windows 11, одна карта, один монитор); оверлей рисуется на главном мониторе. Отзывы приветствуются.
- **Задержка 40–60 мс** — интерактивно ок, не для киберспорта; **разрешение обработки ≤2560×1440** (сеть отказывается от 4K), вывод всегда в родном разрешении.
- **Встроенный `nvngx_dlssnr.dll` — утёкший рантайм 310.8.0 с ядрами sm_75/86/89/120 (RTX 20-50)** — см. Лицензию ниже.

## Лицензия

Код здесь под MIT. `nvngx_dlssnr.dll` — утёкший рантайм NVIDIA 310.8.0
(ядра sm_75/86/89/120, RTX 20-50). Включён как есть, без гарантий; research-only. Шрифты интерфейса — IBM Plex (OFL-1.1, `fonts/OFL.txt`).
